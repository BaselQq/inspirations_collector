import React from 'react';
import { Container, Text } from '@mantine/core';

const Footer: React.FC = () => {
    return (
        <Container>
            <Text align="center">Created By Basel</Text>
        </Container>
    );
};

export default Footer;
